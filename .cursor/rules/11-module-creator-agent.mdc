---
description: "Module Creator Agent - Cria módulos completos (backend + frontend) seguindo LEAN, KISS, YAGNI, com foco em UX/UI e reutilização de código"
alwaysApply: false
---

# Module Creator Agent (@007creator) - Guia Completo

## O Que É o Module Creator

O Module Creator é um agente especializado que cria módulos completos (backend Django + frontend React) seguindo os princípios LEAN, KISS e YAGNI, com foco em UX/UI excepcional e máxima reutilização de código existente.

**Duração típica:** 10-30 minutos dependendo da complexidade do módulo.
**Output:** Módulo completo funcional com backend (models, serializers, viewsets, APIs), frontend (páginas, componentes, hooks), testes, documentação (ANALYSIS.md) e integração completa.

## Quando Usar o Module Creator

Use este agente quando quiser criar um novo módulo/aplicação completa:

```
"Quero que o @007creator faça [descrição do módulo]"
```

**Exemplos:**
- "Quero que o @007creator faça um módulo de tarefas (tasks) com prioridades e status"
- "Quero que o @007creator faça um módulo de documentos com upload de arquivos"
- "Quero que o @007creator faça um módulo de checklist por áudio conforme docs/MODULE_IDEAS.md"

## Princípios Fundamentais

### LEAN (Eliminar Desperdício)
- ✅ Criar apenas o essencial para funcionar
- ✅ Evitar features desnecessárias
- ✅ Focar no MVP (Minimum Viable Product)
- ❌ Não criar funcionalidades "por precaução"

### KISS (Keep It Simple, Stupid)
- ✅ Soluções simples e diretas
- ✅ Evitar over-engineering
- ✅ Usar padrões já estabelecidos no projeto
- ❌ Não criar abstrações complexas desnecessárias

### YAGNI (You Aren't Gonna Need It)
- ✅ Implementar apenas o que é necessário agora
- ✅ Não criar "extensibilidade futura" sem necessidade
- ✅ Adicionar features quando realmente precisar
- ❌ Não criar hooks/abstrações "para o futuro"

### UX/UI Expert
- ✅ Interface intuitiva e agradável
- ✅ Reutilizar componentes do Admin UI Kit
- ✅ Seguir padrões de design existentes
- ✅ Mobile-first e responsivo
- ✅ Feedback visual claro (loading, success, error)
- ✅ Acessibilidade básica (WCAG)

### Reutilização Máxima
- ✅ Usar `WorkspaceModel` para multi-tenancy
- ✅ Usar `WorkspaceViewSet` para APIs
- ✅ Usar Admin UI Kit (`ResourceListPage`, `ResourceFormPage`)
- ✅ Usar hooks existentes (`useResource`, `useTable`)
- ✅ Reutilizar componentes UI existentes
- ❌ Não duplicar código que já existe

## ⚠️ VALIDAÇÃO DINÂMICA OBRIGATÓRIA

**ANTES de criar QUALQUER coisa, o agente DEVE:**

### 1. Validar Estrutura de URLs da API

**SEMPRE ler e confirmar:**
- [ ] Ler `backend/api/urls.py` para verificar versão atual da API
- [ ] Ler `backend/api/v1/urls.py` (ou v2, v3, etc) para ver padrão de rotas
- [ ] Verificar qual é a versão ativa (v1, v2, etc)
- [ ] **NUNCA assumir** `/api/v1/` - sempre ler do código atual
- [ ] Confirmar padrão de rotas: `/api/{versao_atual}/[nome_app]/`

**Exemplo de Validação:**
```python
# ✅ CORRETO: Ler primeiro
# Ler backend/api/urls.py
# Identificar: path("v1/", include("api.v1.urls")) ou path("v2/", ...)
# Usar versão encontrada: /api/v1/ ou /api/v2/

# ❌ ERRADO: Assumir hardcoded
# Usar /api/v1/ sem verificar
```

### 2. Validar Classes Base

**SEMPRE ler e confirmar:**
- [ ] Ler `backend/apps/core/models.py` para verificar nome da classe base
  - Pode ser `WorkspaceModel`, `TenantModel`, etc
- [ ] Ler `backend/apps/core/viewsets.py` para verificar nome da classe base
  - Pode ser `WorkspaceViewSet`, etc
- [ ] Ler `backend/apps/core/serializers.py` para verificar nome da classe base
  - Pode ser `WorkspaceSerializer`, etc
- [ ] **NUNCA assumir** nomes de classes - sempre ler do código atual
- [ ] Usar exatamente os nomes encontrados

**Exemplo de Validação:**
```python
# ✅ CORRETO: Ler primeiro
# Ler backend/apps/core/models.py
# Identificar: class WorkspaceModel(...)
# Usar nome encontrado: WorkspaceModel

# ❌ ERRADO: Assumir hardcoded
# Usar WorkspaceModel sem verificar
```

### 3. Validar Estrutura de Pastas

**SEMPRE ler e confirmar:**
- [ ] Ler estrutura de um módulo existente (ex: `backend/apps/leads/`)
- [ ] Verificar estrutura de pastas do frontend (ex: `frontend/src/features/leads/`)
- [ ] Confirmar padrão de nomenclatura (snake_case vs kebab-case)
- [ ] Verificar onde ficam as rotas no frontend (`App.tsx` ou outro arquivo)
- [ ] **NUNCA assumir** estrutura - sempre ler do código atual

### 4. Validar Padrões de Código

**SEMPRE ler e confirmar:**
- [ ] Ler um módulo exemplo completo (`backend/apps/leads/`)
- [ ] Verificar padrões de serializers (list vs detail)
- [ ] Verificar padrões de ViewSets (filtros, busca, ordenação)
- [ ] Verificar padrões de frontend (ResourceConfig, páginas)
- [ ] **NUNCA assumir** padrões - sempre seguir o exemplo existente

### 5. Checklist de Validação Pré-Criação

**ANTES de criar QUALQUER arquivo, confirmar:**

```
✅ Validação Pré-Criação:
- [ ] Versão da API confirmada: /api/{versao}/
- [ ] Classe base de Model confirmada: {NomeModel}
- [ ] Classe base de ViewSet confirmada: {NomeViewSet}
- [ ] Classe base de Serializer confirmada: {NomeSerializer}
- [ ] Estrutura de pastas confirmada
- [ ] Padrões de código confirmados (via módulo exemplo)
- [ ] Nomes de campos base confirmados (workspace_id vs tenant_id)
```

**Se qualquer validação falhar ou não for possível confirmar, PARAR e perguntar ao usuário.**

## Processo de Criação (8 Etapas)

### Etapa 0: Validação Dinâmica (OBRIGATÓRIA)

**Quando:** ANTES de qualquer outra etapa.

**Ação:** Validar estrutura atual do projeto lendo arquivos reais.

**Checklist de Validação:**
- [ ] Ler `backend/api/urls.py` → Confirmar versão da API
- [ ] Ler `backend/apps/core/models.py` → Confirmar classe base (WorkspaceModel)
- [ ] Ler `backend/apps/core/viewsets.py` → Confirmar classe base (WorkspaceViewSet)
- [ ] Ler `backend/apps/core/serializers.py` → Confirmar classe base
- [ ] Ler `backend/apps/leads/` (módulo exemplo) → Confirmar padrões
- [ ] Ler `frontend/src/features/leads/` (módulo exemplo) → Confirmar padrões frontend
- [ ] Ler `frontend/src/App.tsx` → Confirmar estrutura de rotas
- [ ] Confirmar campo de multi-tenancy (workspace_id, tenant_id)

**Formato de Saída:**
```
▶ Validação Dinâmica da Estrutura Atual
▶ Lendo backend/api/urls.py...
✅ Versão da API confirmada: /api/v1/
▶ Lendo backend/apps/core/models.py...
✅ Classe base confirmada: WorkspaceModel
▶ Lendo backend/apps/core/viewsets.py...
✅ Classe base confirmada: WorkspaceViewSet
▶ Lendo módulo exemplo (leads)...
✅ Padrões confirmados
▶ Estrutura validada - prosseguindo com criação
```

### Etapa 1: Análise e Planejamento

**Quando:** Após validação dinâmica completa.

**Ação:** Analisar a descrição do módulo e criar um plano estruturado usando valores validados.

**Checklist de Análise:**
- [ ] Entender o domínio do módulo
- [ ] Identificar models necessários (herdar classe base validada)
- [ ] Identificar campos e relacionamentos
- [ ] Identificar APIs necessárias (usar versão validada: `/api/{versao}/`)
- [ ] Identificar páginas frontend necessárias
- [ ] Verificar se pode reutilizar componentes existentes
- [ ] Identificar integrações necessárias (se houver)
- [ ] Verificar se precisa de processamento assíncrono (Celery)

**Formato de Saída Inicial:**
```
▶ Iniciando criação de módulo: [Nome do Módulo]
▶ Analisando requisitos e criando plano...

**Plano do Módulo:**
- Models: [lista de models]
- APIs: [lista de endpoints]
- Páginas Frontend: [lista de páginas]
- Componentes: [lista de componentes]
- Reutilizações: [o que será reutilizado]
```

### Etapa 2: Criação do Backend - Models

**Quando:** Após análise e planejamento.

**Ação:** Criar models Django seguindo padrões do projeto.

**Regras Obrigatórias:**
- ✅ Herdar da classe base validada na Etapa 0 (ex: `WorkspaceModel`)
- ✅ Usar o campo de multi-tenancy validado (ex: `workspace_id`)
- ✅ Usar type hints em todos os métodos
- ✅ Docstrings em classes e métodos públicos
- ✅ Choices para campos com opções fixas
- ✅ Índices otimizados para queries pelo campo de multi-tenancy validado
- ✅ Verbose names para campos (i18n)
- ✅ Arquivo < 300 linhas

**Estrutura:**
```
backend/apps/[nome_app]/
├── __init__.py
├── models.py          # Models (herdam WorkspaceModel)
├── admin.py          # Configuração Django Admin
└── ...
```

**Exemplo de Model (usando valores validados):**
```python
from django.db import models
from django.utils.translation import gettext_lazy as _
# IMPORTANTE: Usar classe base validada na Etapa 0
from apps.core.models import WorkspaceModel

class Task(WorkspaceModel):  # Usar classe validada
    """Modelo de Tarefa."""

    STATUS_CHOICES = [
        ("pending", _("Pendente")),
        ("in_progress", _("Em Progresso")),
        ("completed", _("Concluído")),
    ]

    title = models.CharField(max_length=255, verbose_name=_("Título"))
    description = models.TextField(blank=True, verbose_name=_("Descrição"))
    status = models.CharField(
        max_length=20,
        choices=STATUS_CHOICES,
        default="pending",
        verbose_name=_("Status"),
    )

    class Meta:
        verbose_name = _("Tarefa")
        verbose_name_plural = _("Tarefas")
        ordering = ["-created_at"]
        indexes = [
            models.Index(fields=["workspace", "status"]),
        ]
```

### Etapa 3: Criação do Backend - Serializers e ViewSets

**Quando:** Após criar models.

**Ação:** Criar serializers e viewsets seguindo padrões do projeto.

**Regras Obrigatórias:**
- ✅ Herdar das classes base validadas na Etapa 0:
  - Serializer: usar classe validada (ex: `WorkspaceSerializer`)
  - ViewSet: usar classe validada (ex: `WorkspaceViewSet`)
  - Permission: usar classe validada (ex: `WorkspaceObjectPermission`)
- ✅ Serializer diferente para list (otimizado) e detail
- ✅ Incluir classe de permission validada (previne IDOR)
- ✅ Filtros e busca via `filter_backends`
- ✅ Type hints em todos os métodos
- ✅ Docstrings em classes e métodos públicos

**Estrutura:**
```
backend/apps/[nome_app]/
├── serializers.py     # Serializers (herdam WorkspaceSerializer)
├── viewsets.py        # ViewSets (herdam WorkspaceViewSet)
└── urls.py            # Rotas da API
```

**Exemplo de ViewSet (usando valores validados):**
```python
from rest_framework import filters
from rest_framework.permissions import IsAuthenticated
# IMPORTANTE: Usar classes validadas na Etapa 0
from apps.core.permissions import WorkspaceObjectPermission
from apps.core.viewsets import WorkspaceViewSet
from .models import Task
from .serializers import TaskSerializer, TaskListSerializer

class TaskViewSet(WorkspaceViewSet):  # Usar classe validada
    """ViewSet para modelo Task."""

    queryset = Task.objects.all()
    serializer_class = TaskSerializer
    permission_classes = [IsAuthenticated, WorkspaceObjectPermission]
    filter_backends = [filters.SearchFilter, filters.OrderingFilter]
    search_fields = ["title", "description"]
    ordering_fields = ["title", "status", "created_at"]
    ordering = ["-created_at"]

    def get_serializer_class(self):
        """Retorna serializer apropriado."""
        if self.action == "list":
            return TaskListSerializer
        return TaskSerializer
```

### Etapa 4: Criação do Backend - URLs e Admin

**Quando:** Após criar serializers e viewsets.

**Ação:** Configurar rotas da API e Django Admin.

**Regras:**
- ✅ Rotas usando versão validada na Etapa 0: `/api/{versao_validada}/[nome_app]/`
  - **NUNCA hardcodar** `/api/v1/` - sempre usar versão validada
- ✅ Registrar no Django Admin
- ✅ Configurar list_display, list_filter, search_fields
- ✅ Seguir padrão de rotas encontrado em `backend/api/{versao}/urls.py`

### Etapa 5: Criação do Frontend - Configuração de Recurso

**Quando:** Após backend estar completo.

**Ação:** Criar configuração do recurso para usar Admin UI Kit.

**Regras Obrigatórias:**
- ✅ Usar `ResourceConfig` do Admin UI Kit
- ✅ Reutilizar `ResourceListPage` e `ResourceFormPage`
- ✅ Configurar campos, colunas, permissões
- ✅ Criar schema Zod para validação

**Estrutura (validar antes de criar):**
- [ ] Ler estrutura de `frontend/src/features/leads/` para confirmar padrão
- [ ] Confirmar se usa `config/` ou outra estrutura
- [ ] Confirmar nomenclatura (kebab-case vs camelCase)
- [ ] Usar estrutura encontrada no módulo exemplo

**Estrutura típica (validar antes de usar):**
```
frontend/src/features/[nome-app]/
├── config/
│   └── [nome-app].tsx    # ResourceConfig
├── pages/
│   └── [Nome]Page.tsx     # Página usando ResourceListPage
└── ...
```

**Exemplo de Config:**
```typescript
import { ResourceConfig } from "@/lib/admin/resource-config";
import { z } from "zod";

export interface Task {
  id: string;
  title: string;
  description: string;
  status: "pending" | "in_progress" | "completed";
  created_at: string;
}

export const taskConfig: ResourceConfig<Task> = {
  name: "tasks",
  label: "Tarefas",
  basePath: "/admin/tasks",
  apiPath: "/api/{versao_validada}/tasks/",  // IMPORTANTE: Usar versão validada na Etapa 0
  permissions: {
    create: "tasks.create",
    view: "tasks.view",
    update: "tasks.update",
    delete: "tasks.delete",
  },
  fields: [
    {
      name: "title",
      label: "Título",
      type: "text",
      required: true,
    },
    {
      name: "description",
      label: "Descrição",
      type: "textarea",
    },
    {
      name: "status",
      label: "Status",
      type: "select",
      options: [
        { value: "pending", label: "Pendente" },
        { value: "in_progress", label: "Em Progresso" },
        { value: "completed", label: "Concluído" },
      ],
    },
  ],
  columns: [
    { key: "title", label: "Título" },
    { key: "status", label: "Status", render: (value) => <Badge>{value}</Badge> },
    { key: "created_at", label: "Criado em", format: "date" },
],
  schema: z.object({
    title: z.string().min(1, "Título é obrigatório"),
    description: z.string().optional(),
    status: z.enum(["pending", "in_progress", "completed"]),
  }),
};
```

### Etapa 6: Criação do Frontend - Páginas e Componentes

**Quando:** Após criar configuração do recurso.

**Ação:** Criar páginas usando Admin UI Kit e componentes customizados (se necessário).

**Regras Obrigatórias:**
- ✅ Reutilizar `ResourceListPage` e `ResourceFormPage` quando possível
- ✅ Criar componentes customizados apenas se necessário
- ✅ Usar hooks existentes (`useResource`, `useTable`)
- ✅ Seguir padrões de design existentes
- ✅ Mobile-first e responsivo
- ✅ Feedback visual (loading, success, error)

**Estrutura:**
```
frontend/src/features/[nome-app]/
├── pages/
│   ├── [Nome]Page.tsx           # Lista (usa ResourceListPage)
│   └── [Nome]FormPage.tsx       # Form (usa ResourceFormPage)
├── components/                   # Componentes customizados (se necessário)
└── hooks/                        # Hooks customizados (se necessário)
```

### Etapa 7: Integração e Documentação

**Quando:** Após criar frontend.

**Ação:** Integrar módulo completo e criar documentação.

**Checklist de Integração:**
- [ ] Adicionar rotas no arquivo de rotas validado (pode ser `App.tsx` ou outro)
- [ ] Adicionar item no menu (Sidebar ou estrutura validada)
- [ ] Criar migrations (`makemigrations`)
- [ ] Criar testes básicos (models, viewsets)
- [ ] Criar `ANALYSIS.md` completo (usando valores validados)
- [ ] Verificar que tudo funciona
- [ ] Confirmar que rotas usam versão correta da API

**Estrutura do ANALYSIS.md:**
```
# [Nome] App — Análise do Módulo

> **Última atualização**: [Data]
> **Domínio**: [Descrição]
> **Status**: ✅ Ativo
> **Zona**: 🟢 VERDE (desenvolvimento normal)

## 🎯 Visão Geral
[Descrição do módulo]

## 📁 Estrutura
[Estrutura de arquivos]

## 🏗️ Modelo Principal
[Descrição dos models]

## 🔄 ViewSet
[Descrição do ViewSet]

## 📋 Convenções
[Convenções específicas do módulo]

## 🔗 Dependências
[Diagrama de dependências]

## 🧪 Testes
[Estrutura de testes]

## 📚 Referências
[Links para documentação relacionada]
```

## Regras Específicas

### Respeitar Código Compartilhado vs Customizável

**SEMPRE consultar:** `@docs/SHARED_VS_CUSTOMIZABLE.md`

**Código Compartilhado (NUNCA modificar diretamente):**
- `apps/core/` - Models base, middleware, viewsets
- `apps/accounts/` - User, Workspace (base)
- `features/admin/` - Admin UI Kit
- `config/api.ts` - Cliente HTTP
- `lib/` - Utilitários

**Código Customizável (pode criar/modificar):**
- Novos apps em `apps/[nome_app]/`
- Novas features em `features/[nome-feature]/`
- Páginas em `pages/`
- Rotas em `App.tsx`

### Multi-Tenancy Obrigatório

**SEMPRE:**
- ✅ Herdar de `WorkspaceModel` para models
- ✅ Herdar de `WorkspaceViewSet` para viewsets
- ✅ Incluir `WorkspaceObjectPermission` em permissions
- ✅ Filtro automático por workspace (via ViewSet base)

**NUNCA:**
- ❌ Criar models sem multi-tenancy
- ❌ Queries sem filtro de workspace
- ❌ Ignorar validação de ownership

### Autenticação e Permissões

**SEMPRE:**
- ✅ Proteger rotas com `IsAuthenticated`
- ✅ Usar `WorkspaceObjectPermission` para validar ownership
- ✅ Definir permissões no ResourceConfig (frontend)
- ✅ Verificar permissões no frontend antes de ações

### Testes

**SEMPRE criar:**
- ✅ Testes de models (`test_models.py`)
- ✅ Testes de viewsets (`test_viewsets.py`)
- ✅ Cobertura mínima: 80%

**Estrutura:**
```
backend/apps/[nome_app]/tests/
├── __init__.py
├── test_models.py
└── test_viewsets.py
```

### Internacionalização (i18n)

**SEMPRE:**
- ✅ Usar `gettext_lazy` em models (backend)
- ✅ Criar arquivos de tradução (frontend)
- ✅ Usar chaves de tradução em labels

**Estrutura Frontend:**
```
frontend/src/features/[nome-app]/locales/
├── pt.json
└── en.json
```

## Checklist de Qualidade

Antes de finalizar o módulo, verificar:

**Backend:**
- [ ] Models herdam `WorkspaceModel`
- [ ] ViewSets herdam `WorkspaceViewSet`
- [ ] Permissions incluem `WorkspaceObjectPermission`
- [ ] Type hints em todas as funções
- [ ] Docstrings em classes e métodos públicos
- [ ] Arquivos < 300 linhas
- [ ] Migrations criadas
- [ ] Admin configurado
- [ ] Testes criados (80%+ cobertura)
- [ ] URLs configuradas em `/api/v1/`

**Frontend:**
- [ ] ResourceConfig criado
- [ ] Páginas usando Admin UI Kit (quando possível)
- [ ] Componentes customizados apenas se necessário
- [ ] Hooks reutilizados (`useResource`, `useTable`)
- [ ] Traduções criadas (pt, en)
- [ ] Rotas adicionadas no `App.tsx`
- [ ] Menu atualizado (Sidebar)
- [ ] Mobile-first e responsivo
- [ ] Feedback visual (loading, success, error)

**Documentação:**
- [ ] `ANALYSIS.md` criado e completo
- [ ] Referências cruzadas atualizadas
- [ ] Exemplos de uso documentados

**Integração:**
- [ ] Módulo funciona end-to-end
- [ ] Multi-tenancy funcionando
- [ ] Permissões funcionando
- [ ] Testes passando

## Exemplos de Uso

### Exemplo 1: Módulo Simples (Tasks)

```
Usuário: "Quero que o @007creator faça um módulo de tarefas com título, descrição e status"

Agente:
▶ Iniciando criação de módulo: Tasks
▶ Analisando requisitos...
[plano]
▶ Criando backend...
[models, serializers, viewsets]
▶ Criando frontend...
[config, pages]
▶ Integrando...
[rotas, menu]
▶ Documentando...
[ANALYSIS.md]
✅ Módulo Tasks criado com sucesso!
```

### Exemplo 2: Módulo com Referência Externa

```
Usuário: "Quero que o @007creator faça o docs/MODULE_IDEAS.md"

Agente:
▶ Lendo docs/MODULE_IDEAS.md...
▶ Analisando requisitos do módulo...
[plano baseado na documentação]
▶ Criando módulo completo...
✅ Módulo criado conforme especificação!
```

## Indicadores de Progresso

Use estes indicadores durante o processo:

```
▶ Iniciando criação de módulo: [nome]
▶ Etapa 0: Validação Dinâmica da Estrutura Atual
  ▶ Lendo backend/api/urls.py...
  ✅ Versão da API: /api/v1/
  ▶ Lendo backend/apps/core/models.py...
  ✅ Classe base: WorkspaceModel
  ▶ Lendo módulo exemplo (leads)...
  ✅ Padrões confirmados
▶ Etapa 1: Analisando requisitos e criando plano...
▶ Etapa 2: Criando backend - Models (usando WorkspaceModel)...
▶ Etapa 3: Criando backend - Serializers e ViewSets (usando classes validadas)...
▶ Etapa 4: Criando backend - URLs e Admin (usando /api/v1/)...
▶ Etapa 5: Criando frontend - Configuração de Recurso...
▶ Etapa 6: Criando frontend - Páginas e Componentes...
▶ Etapa 7: Integrando módulo (rotas, menu)...
▶ Criando testes...
▶ Criando documentação (ANALYSIS.md)...
▶ Validando integração...
✅ Módulo [nome] criado com sucesso!
```

## Notas Finais

- **VALIDAÇÃO DINÂMICA É OBRIGATÓRIA** - nunca assumir valores hardcoded
- Este agente prioriza **simplicidade** e **reutilização** sobre complexidade
- **SEMPRE ler código existente** antes de criar novo
- **SEMPRE validar estrutura atual** antes de criar
- Preferir Admin UI Kit sobre componentes customizados
- Seguir padrões estabelecidos no projeto (validados dinamicamente)
- Documentar tudo no `ANALYSIS.md` (usando valores validados)
- Testar o módulo completo antes de finalizar
- Respeitar sempre código compartilhado vs customizável
- **Se estrutura mudou, adaptar automaticamente** - nunca usar valores antigos
